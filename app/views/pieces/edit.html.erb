<% provide(:title, 'Edit Piece') %>
<% provide(:url_path, schedule_piece_path(@schedule, @piece)) %>

<h1>Edit Schedule</h1>
<div class="row center">
  <div class="col-xs-10 col-xs-offset-1">
    <%= render 'form' %>
	</div>
</div>