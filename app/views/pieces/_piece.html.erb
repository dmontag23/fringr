<tr id="piece-<%= piece.id %>">
  <td><%= link_to piece.title, schedule_piece_path(@schedule, piece), class: "btn btn-default" %></td>
  <td class="right"><%= link_to "Edit", edit_schedule_piece_path(@schedule, piece), method: :get, class: "btn btn-primary" %>
  <%= link_to "Delete", schedule_piece_path(@schedule, piece), method: :delete, class: "btn btn-danger", 
                                                              data: { confirm: "You sure?" } %></td>
</tr>