<li id="piece-<%= piece.id %>">
  <%= link_to piece.title, schedule_piece_path(@schedule, piece) %>
  <%= link_to "Edit", edit_schedule_piece_path(@schedule, piece), method: :get %>
  <%= link_to "Delete", schedule_piece_path(@schedule, piece), method: :delete,
                                                              data: { confirm: "You sure?" } %>
</li>