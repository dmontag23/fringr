<div class="navbar navbar-fixed-top navbar-inverse">
	<div class="container">
	<%= link_to "Fringr", root_path, class: "navbar-brand" %>
		<ul class="nav navbar-nav navbar-right">
			<li><%= link_to "Home", root_path %></li>
			<% if logged_in? %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">
            Settings<b class="caret"></b>
          </a>
          <ul class="dropdown-menu">
            <li><%= link_to "Edit Password", edit_password_reset_path(current_user) %></li>
            <li class="divider"></li>
            <li><%= link_to "Delete Account", user_path(current_user), method: :delete, 
                    data: { confirm: "Are you sure? Your entire account will be deleted and cannot be recovered." } %> </li>
          </ul>
        </li>
				<li><%= link_to "Log out", logout_path, method: "delete" %></li>
			<% else %>
				<li><%= link_to "Log In", login_path %></li>
			<% end %>
		</ul>
	</div>
</div>