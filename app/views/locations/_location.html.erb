<tr id="location-<%= location.id %>">
  <td><%= location.name %></td>
  <td class="right"><%= button_to "Delete", location_path(location), method: :delete, class: "btn btn-danger", 
                          data: { confirm: "Are you sure? All corresponding locations will be removed from your schedules." } %></td>
</tr>